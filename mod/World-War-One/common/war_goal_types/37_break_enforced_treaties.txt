REPLACE_OR_CREATE:break_enforced_treaties = {
	icon = "gfx/interface/icons/war_goals/enforce_treaty_article.dds"

	kind = break_enforced_treaties

	settings = {
		require_target_be_part_of_war
		skip_build_list

		# Validation
		validate_conflicts_war_goals_all
	}

	contestion_type = control_any_target_country_state

	target_type = country

	possible = {
		always = no
		NOR = {
			has_law = law_type:law_true_neutrality
			has_law = law_type:law_armed_neutrality
		}
	}

	valid = {
	}

	maneuvers = {
		value = 0
	}

	infamy = {
		value = 0
	}

	on_enforced = {
		# script effect on top of the predefined code effect
	}

	mirrored_wargoal = {
		method = no_mirror
	}

	ai = {
		is_significant_demand = no
	}
}
