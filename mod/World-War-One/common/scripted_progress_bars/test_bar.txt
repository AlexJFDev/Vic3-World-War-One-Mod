test_progress_bar = {
    name = test_bar_name
    desc = test_bar_desc
    second_desc = test_bar_second_desc

    start_value = 0
    min_value = -100
    max_value = 100

    double_sided_gold = yes

    weekly_progress = {
 		add = {
 			desc = test_bar_add_weekly_desc
 			value = 1
 		}
 	}
}